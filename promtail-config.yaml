server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://sd_sys_loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: citadel_threats
    static_configs:
      - targets: [localhost]
        labels:
          job: citadel_threats
          # 🚨 FORCE THE TRACKER TO READ NATIVE TEXT MOUNT VOLUMES DIRECTLY
          __path__: /app/logs/breach_alerts.log
